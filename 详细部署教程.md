# 🚀 详细部署教程 - 手把手教你部署

## 📋 部署前准备检查

### 步骤1：检查必需文件

确保以下文件都存在：
- ✅ `Dockerfile` - Docker配置文件
- ✅ `app.py` - 主应用文件
- ✅ `rf_model.pkl` - 训练好的模型（约15MB）
- ✅ `shap_explainer.pkl` - SHAP解释器（约20MB）
- ✅ `feature_info.pkl` - 特征信息（很小）
- ✅ `requirements.txt` - Python依赖

如果缺少模型文件，先运行：
```bash
python train_model.py
```

---

## 🎯 方法一：Railway部署（最简单，推荐）

### 第一步：准备GitHub仓库

1. **如果没有GitHub账号，先注册：**
   - 访问 https://github.com
   - 点击右上角 "Sign up"
   - 完成注册

2. **创建新仓库：**
   - 登录GitHub
   - 点击右上角 "+" → "New repository"
   - 仓库名称：`pressure-injuries-in-sepsis`（或你喜欢的名字）
   - 选择 Public（公开）
   - 不要勾选 "Initialize with README"
   - 点击 "Create repository"

3. **上传代码到GitHub：**

   打开命令行（PowerShell或CMD），执行：

   ```bash
   # 进入项目目录
   cd F:\Data_Analysis\meeting
   
   # 初始化Git（如果还没有）
   git init
   
   # 添加所有文件
   git add .
   
   # 提交
   git commit -m "Initial commit - Pressure Injuries in Sepsis Calculator"
   
   # 连接到GitHub仓库（替换YOUR_USERNAME为你的GitHub用户名）
   git remote add origin https://github.com/YOUR_USERNAME/pressure-injuries-in-sepsis.git
   
   # 推送到GitHub
   git branch -M main
   git push -u origin main
   ```

   **注意：** 如果文件太大（.pkl文件），可能需要使用Git LFS：
   ```bash
   # 安装Git LFS（如果还没有）
   git lfs install
   
   # 跟踪大文件
   git lfs track "*.pkl"
   
   # 然后重新添加和提交
   git add .gitattributes
   git add .
   git commit -m "Add model files with LFS"
   git push -u origin main
   ```

---

### 第二步：在Railway上部署

1. **注册Railway账号：**
   - 访问 https://railway.app
   - 点击右上角 "Login"
   - 选择 "Login with GitHub"
   - 授权Railway访问你的GitHub账号

2. **创建新项目：**
   - 登录后，点击 "New Project"
   - 选择 "Deploy from GitHub repo"
   - 在列表中找到你的仓库 `pressure-injuries-in-sepsis`
   - 点击仓库名称

3. **Railway自动部署：**
   - Railway会自动检测到 `Dockerfile`
   - 开始构建Docker镜像（需要几分钟）
   - 等待构建完成（会显示日志）

4. **获取访问链接：**
   - 构建完成后，点击项目
   - 在 "Settings" 标签页
   - 找到 "Domains" 部分
   - 会看到一个URL，类似：`https://your-app-name.up.railway.app`
   - 这就是你的网站链接！

5. **自定义域名（可选）：**
   - 在 "Domains" 部分点击 "Generate Domain"
   - 可以自定义名称，如：`pressure-injuries-in-sepsis`
   - 最终链接：`https://pressure-injuries-in-sepsis.up.railway.app`

---

## 🎯 方法二：Render部署（备选方案）

如果Railway有问题，可以用Render：

1. **注册Render账号：**
   - 访问 https://render.com
   - 点击 "Get Started for Free"
   - 选择 "Sign up with GitHub"

2. **创建Web服务：**
   - 登录后，点击 "New +"
   - 选择 "Web Service"
   - 连接你的GitHub仓库

3. **配置服务：**
   - **Name:** `pressure-injuries-in-sepsis`
   - **Environment:** 选择 "Docker"
   - **Region:** 选择离你最近的（如 Singapore）
   - **Branch:** `main`
   - **Root Directory:** 留空
   - **Docker Build Context:** 留空
   - **Dockerfile Path:** `Dockerfile`
   - **Port:** `8501`

4. **部署：**
   - 点击 "Create Web Service"
   - 等待构建完成（5-10分钟）
   - 完成后会显示URL

---

## 🔍 常见问题解决

### 问题1：Git推送失败（文件太大）

**解决方案：使用Git LFS**
```bash
# 安装Git LFS
git lfs install

# 跟踪大文件
git lfs track "*.pkl"

# 重新提交
git add .gitattributes
git add .
git commit -m "Add files with LFS"
git push
```

### 问题2：构建失败

**检查：**
1. 所有.pkl文件是否在仓库中
2. Dockerfile是否正确
3. requirements.txt是否完整

**查看日志：**
- Railway: 点击项目 → "Deployments" → 查看日志
- Render: 点击服务 → "Logs" 标签

### 问题3：应用无法访问

**检查：**
1. 部署是否完成（状态显示为"Running"）
2. 端口是否正确（8501）
3. 查看错误日志

---

## 📝 部署后检查清单

部署完成后，检查：

- [ ] 网站可以打开
- [ ] 可以输入特征变量
- [ ] 点击计算按钮有响应
- [ ] 显示风险评分
- [ ] SHAP图表正常显示

---

## 🆘 需要帮助？

如果遇到问题：

1. **查看平台日志：**
   - Railway: 项目 → Deployments → 点击最新部署 → 查看日志
   - Render: 服务 → Logs 标签

2. **常见错误：**
   - "Model file not found" → 检查.pkl文件是否上传
   - "Port already in use" → 检查端口配置
   - "Build failed" → 查看构建日志

3. **重新部署：**
   - 修改代码后，推送到GitHub
   - 平台会自动重新部署

---

## 🎉 部署成功！

部署成功后，你会得到一个链接，例如：
- `https://pressure-injuries-in-sepsis.up.railway.app`
- 或 `https://pressure-injuries-in-sepsis.onrender.com`

将这个链接分享给其他人，他们就可以访问你的计算器了！

---

## 💡 提示

- **免费额度：** Railway每月有$5免费额度，Render有免费套餐
- **自动部署：** 每次推送代码到GitHub，平台会自动重新部署
- **自定义域名：** 可以绑定自己的域名（需要购买域名）

---

## 📞 下一步

部署完成后：
1. 测试所有功能
2. 分享链接给需要的人
3. 如果需要，可以自定义域名

祝你部署顺利！🎊

