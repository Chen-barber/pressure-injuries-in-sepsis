# 完整部署步骤 - 从零开始

## 你的GitHub仓库
✅ 已创建：https://github.com/Chen-barber/pressure-injuries-in-sepsis

---

## 第一步：安装Git

### 方法1：使用winget安装（推荐）

以**管理员身份**打开PowerShell，运行：

```powershell
winget install --id Git.Git -e --source winget
```

等待安装完成（约2-3分钟）

### 方法2：手动下载安装

1. 访问：https://git-scm.com/download/win
2. 下载 "64-bit Git for Windows Setup"
3. 运行安装程序
4. **重要：** 安装时确保勾选 "Add Git to PATH"

### 安装后验证

**关闭并重新打开PowerShell**，然后运行：

```powershell
git --version
```

如果显示版本号（如 `git version 2.xx.x`），说明安装成功！

---

## 第二步：安装Git LFS（用于大文件）

你的.pkl文件很大（约35MB），需要使用Git LFS：

```powershell
winget install --id Git.Git-LFS -e --source winget
```

或访问：https://git-lfs.github.com/ 手动下载安装

---

## 第三步：配置Git（首次使用）

```powershell
git config --global user.name "Chen-barber"
git config --global user.email "你的GitHub邮箱"
```

---

## 第四步：上传代码到GitHub

在项目目录（F:\Data_Analysis\meeting）运行以下命令：

```powershell
# 1. 进入项目目录（如果还没在）
cd F:\Data_Analysis\meeting

# 2. 初始化Git仓库
git init

# 3. 配置Git LFS（用于大文件）
git lfs install
git lfs track "*.pkl"

# 4. 添加.gitattributes文件
git add .gitattributes

# 5. 添加所有文件
git add .

# 6. 提交
git commit -m "Initial commit - Pressure Injuries in Sepsis Calculator"

# 7. 连接到你的GitHub仓库
git remote add origin https://github.com/Chen-barber/pressure-injuries-in-sepsis.git

# 8. 设置主分支
git branch -M main

# 9. 推送到GitHub
git push -u origin main
```

**注意：** 第9步会要求输入：
- Username: `Chen-barber`
- Password: **不是GitHub密码！** 需要使用Personal Access Token（见下方）

---

## 第五步：创建Personal Access Token

GitHub不再支持密码登录，需要创建Token：

1. **访问：** https://github.com/settings/tokens
2. **点击：** "Generate new token" → "Generate new token (classic)"
3. **设置：**
   - Note: `Deploy Pressure Injuries in Sepsis`
   - Expiration: 选择期限（建议90天或No expiration）
   - **勾选权限：** `repo`（全部权限）
4. **点击：** "Generate token"
5. **重要：** 立即复制token（类似：`ghp_xxxxxxxxxxxxxxxxxxxx`），只显示一次！

6. **推送时使用：**
   - Username: `Chen-barber`
   - Password: **粘贴刚才复制的token**

---

## 第六步：验证上传

访问你的仓库：
https://github.com/Chen-barber/pressure-injuries-in-sepsis

应该能看到所有文件了！

---

## 第七步：部署到Railway

代码上传成功后，部署到Railway：

1. **访问：** https://railway.app
2. **点击：** "Login" → "Login with GitHub"
3. **授权：** 允许Railway访问你的GitHub
4. **创建项目：**
   - 点击 "New Project"
   - 选择 "Deploy from GitHub repo"
   - 找到并选择：`Chen-barber/pressure-injuries-in-sepsis`
5. **等待部署：**
   - Railway会自动检测Dockerfile
   - 开始构建（约3-5分钟）
   - 查看构建日志
6. **获取链接：**
   - 部署完成后，点击项目
   - Settings → Domains
   - 会看到一个URL，例如：`https://pressure-injuries-in-sepsis.up.railway.app`
   - 这就是你的网站链接！

---

## 常见问题

### Q1: git命令找不到
**解决：** 安装Git后，**必须关闭并重新打开PowerShell**

### Q2: git lfs命令找不到
**解决：** 安装Git LFS：
```powershell
winget install --id Git.Git-LFS -e --source winget
```

### Q3: 推送失败 - 认证错误
**解决：** 使用Personal Access Token代替密码（见第五步）

### Q4: 推送失败 - 文件太大
**解决：** 确保使用了Git LFS：
```powershell
git lfs install
git lfs track "*.pkl"
git add .gitattributes
```

### Q5: Railway构建失败
**解决：** 
- 检查所有.pkl文件是否上传成功
- 查看Railway的构建日志
- 确认Dockerfile存在

---

## 快速命令总结

```powershell
# 安装Git
winget install --id Git.Git -e --source winget

# 安装Git LFS
winget install --id Git.Git-LFS -e --source winget

# 配置Git
git config --global user.name "Chen-barber"
git config --global user.email "你的邮箱"

# 上传代码
cd F:\Data_Analysis\meeting
git init
git lfs install
git lfs track "*.pkl"
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/Chen-barber/pressure-injuries-in-sepsis.git
git branch -M main
git push -u origin main
```

---

## 需要帮助？

如果遇到问题，告诉我：
1. 在哪一步遇到问题
2. 具体的错误信息
3. 我会帮你解决！

祝你部署顺利！🎉

