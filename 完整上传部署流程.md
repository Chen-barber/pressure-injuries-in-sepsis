# 完整上传部署流程 - 最新版

## 📋 第一步：检查必需文件

确保以下文件都在 `F:\Data_Analysis\meeting` 目录：

### 必需文件清单：
- ✅ `Dockerfile` - Docker配置文件
- ✅ `app.py` - 主应用文件
- ✅ `requirements.txt` - Python依赖
- ✅ `rf_model.pkl` - 训练好的模型（约14MB）
- ✅ `shap_explainer.pkl` - SHAP解释器（约20MB）
- ✅ `feature_info.pkl` - 特征信息（很小）

### 可选但有用的文件：
- `docker-compose.yml` - 本地测试用
- `railway.json` - Railway配置
- `render.yaml` - Render配置
- `README.md` - 说明文档

### 不需要上传的文件：
- `train_model.py` - 训练脚本（可选，部署不需要）
- `generate_shap_images.py` - 生成图片脚本（可选）
- `create_shap_plots.py` - 生成图片脚本（可选）
- `*.bat` - Windows批处理文件（可选）
- `*.xlsx` - Excel文件（不需要）
- `*.csv` - CSV文件（不需要）
- 其他数据文件夹

---

## 🚀 第二步：清理并重新初始化Git

### 2.1 删除旧的Git配置（如果存在）

```powershell
cd F:\Data_Analysis\meeting

# 如果.git文件夹存在，删除它
if (Test-Path ".git") {
    Remove-Item -Recurse -Force .git
    Write-Host "已删除旧的Git配置" -ForegroundColor Yellow
}
```

### 2.2 创建.gitignore文件（排除不需要的文件）

创建 `.gitignore` 文件，内容：

```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
*.egg-info/

# 数据文件（不需要上传）
*.xlsx
*.csv
*.xls
spesis and pi/
meeting/
Critical Care/
Final result终版/

# 临时文件
*.log
*.tmp
.DS_Store

# IDE
.vscode/
.idea/
*.swp

# 但保留必需的文件
!rf_model.pkl
!shap_explainer.pkl
!feature_info.pkl
!requirements.txt
!app.py
!Dockerfile
```

---

## 📤 第三步：初始化Git并上传

### 3.1 初始化Git仓库

```powershell
cd F:\Data_Analysis\meeting

# 初始化Git
git init

# 配置Git（如果还没配置）
git config user.name "Chen-barber"
git config user.email "17681978112@163.com"
```

### 3.2 安装并配置Git LFS（用于大文件）

```powershell
# 检查Git LFS是否已安装
git lfs version

# 如果显示"command not found"，先安装：
winget install --id Git.Git-LFS -e --source winget

# 安装后关闭并重新打开PowerShell，然后：
git lfs install
git lfs track "*.pkl"
```

### 3.3 添加并提交文件

```powershell
# 添加.gitattributes（Git LFS配置）
git add .gitattributes

# 添加.gitignore
git add .gitignore

# 添加所有必需文件
git add Dockerfile
git add app.py
git add requirements.txt
git add rf_model.pkl
git add shap_explainer.pkl
git add feature_info.pkl

# 可选：添加配置文件
git add docker-compose.yml
git add railway.json
git add render.yaml
git add README.md

# 提交
git commit -m "Deploy Pressure Injuries in Sepsis Calculator"
```

### 3.4 连接到GitHub仓库

```powershell
# 连接到你的GitHub仓库
git remote add origin https://github.com/Chen-barber/pressure-injuries-in-sepsis.git

# 如果之前已经连接过，先删除再添加：
# git remote remove origin
# git remote add origin https://github.com/Chen-barber/pressure-injuries-in-sepsis.git
```

### 3.5 推送到GitHub

```powershell
# 设置主分支
git branch -M main

# 配置Git以处理大文件
git config http.postBuffer 524288000
git config http.maxRequestBuffer 100M

# 推送到GitHub
git push -u origin main
```

**注意：** 推送时会要求输入：
- Username: `Chen-barber`
- Password: **使用Personal Access Token**（不是GitHub密码）

---

## 🔑 第四步：创建Personal Access Token（如果还没有）

1. **访问：** https://github.com/settings/tokens
2. **点击：** "Generate new token" → "Generate new token (classic)"
3. **设置：**
   - Note: `Deploy Pressure Injuries in Sepsis`
   - Expiration: 选择期限（建议90天或No expiration）
   - **勾选权限：** `repo`（全部权限）
4. **点击：** "Generate token"
5. **立即复制token**（类似：`ghp_xxxxxxxxxxxxxxxxxxxx`），只显示一次！
6. **推送时使用：**
   - Username: `Chen-barber`
   - Password: **粘贴刚才复制的token**

---

## ✅ 第五步：验证上传

访问你的GitHub仓库：
**https://github.com/Chen-barber/pressure-injuries-in-sepsis**

检查应该能看到：
- ✅ Dockerfile
- ✅ app.py
- ✅ requirements.txt
- ✅ rf_model.pkl
- ✅ shap_explainer.pkl
- ✅ feature_info.pkl

如果所有文件都在，说明上传成功！

---

## 🚀 第六步：部署到Railway

### 6.1 登录Railway

1. **访问：** https://railway.app
2. **点击：** 右上角 "Login"
3. **选择：** "Login with GitHub"
4. **授权：** 允许Railway访问你的GitHub账号

### 6.2 创建新项目

1. **点击：** "New Project"
2. **选择：** "Deploy from GitHub repo"
3. **在列表中找到：** `Chen-barber/pressure-injuries-in-sepsis`
4. **点击：** 选择这个仓库

### 6.3 等待自动部署

Railway会自动：
- ✅ 检测到Dockerfile
- ✅ 开始构建Docker镜像（约3-5分钟）
- ✅ 显示构建日志
- ✅ 部署应用

### 6.4 获取网站链接

部署完成后：

1. **点击：** 你的项目
2. **点击：** "Settings" 标签
3. **找到：** "Domains" 部分
4. **会看到：** 一个URL，例如：
   - `https://pressure-injuries-in-sepsis.up.railway.app`
   - 或 `https://your-app-name.up.railway.app`

5. **自定义域名（可选）：**
   - 点击 "Generate Domain"
   - 可以自定义名称
   - 例如：`pressure-injuries-in-sepsis`

**这就是你的网站链接！** 可以分享给其他人了！

---

## 🔍 第七步：测试网站

1. **打开链接**（Railway提供的URL）
2. **测试功能：**
   - 输入特征变量
   - 点击 "Calculate Sepsis Risk"
   - 查看风险评分
   - 查看SHAP可视化

---

## 🆘 常见问题解决

### 问题1：推送失败 - 文件太大

**解决：**
```powershell
git config http.postBuffer 524288000
git config http.maxRequestBuffer 100M
git push -u origin main
```

### 问题2：推送失败 - 认证错误

**解决：** 使用Personal Access Token代替密码（见第四步）

### 问题3：Railway构建失败

**检查：**
1. 所有.pkl文件是否上传成功
2. Dockerfile是否正确
3. 查看Railway的构建日志

### 问题4：应用无法访问

**检查：**
1. 部署状态是否为"Running"
2. 查看应用日志
3. 确认端口8501已暴露

---

## 📝 快速命令总结（复制粘贴）

```powershell
# 1. 进入项目目录
cd F:\Data_Analysis\meeting

# 2. 删除旧Git配置（如果需要）
if (Test-Path ".git") { Remove-Item -Recurse -Force .git }

# 3. 初始化Git
git init
git config user.name "Chen-barber"
git config user.email "17681978112@163.com"

# 4. 配置Git LFS
git lfs install
git lfs track "*.pkl"

# 5. 添加文件
git add .gitattributes
git add .gitignore
git add Dockerfile app.py requirements.txt
git add rf_model.pkl shap_explainer.pkl feature_info.pkl

# 6. 提交
git commit -m "Deploy Pressure Injuries in Sepsis Calculator"

# 7. 连接GitHub
git remote add origin https://github.com/Chen-barber/pressure-injuries-in-sepsis.git

# 8. 配置大文件推送
git config http.postBuffer 524288000
git config http.maxRequestBuffer 100M

# 9. 推送
git branch -M main
git push -u origin main
```

---

## 🎉 完成！

上传成功后，按照第六步部署到Railway，就能获得在线网站链接了！

如果遇到问题，告诉我具体在哪一步，我会帮你解决！

